/**
 * Created by max1m on 01.10.20.
 */

({
    doInit: function (component, event, helper) {
        helper.doInit(component, event, helper);
    },

    save: function (component, event, helper) {
        helper.save(component, event, helper);
    },

    cancel: function (component, event, helper) {
        helper.cancel(component, event, helper);
    },

    next: function (component, event, helper) {
        helper.next(component, event, helper);
    },

    back: function (component, event, helper) {
        helper.back(component, event, helper);
    },

    schedulerInvoicesCreate: function (component, event, helper) {
        helper.schedulerInvoicesCreate(component, event, helper);
    }
});